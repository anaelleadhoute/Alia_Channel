// Copy to config.js (gitignored) and set this to match ADMIN_API_KEY in the
// backend's .env. Generate a value with:
//   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
window.ADMIN_KEY = "CHANGE_ME";
